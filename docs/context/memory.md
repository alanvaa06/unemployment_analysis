# Memory

- decision: Project pivoted to BLS-based granular unemployment + AI-impact tracker; deliverable target = interactive HTML/Plotly report, descriptive AI analysis first then econometric layer. (PRD.md momentum-strategy is unrelated legacy.)
- decision: BLS series catalog (all programs) lives in `docs/data/bls_series_catalog.md`; series-gathering done, pipeline build deferred per user.
- fact: BLS JSON API v2 works for scripted access; bls.gov HTML + download.bls.gov flat files are bot-blocked (HTTP 403) — use the API, not scraping.
- fact: BLS API key is emailed at registration (data.bls.gov/registrationEngine), no dashboard; recover by re-registering same email. Store as env `BLS_API_KEY`.
- decision: AI-impact key cuts = CPS education gradient (bachelor's+ `LNS14027662` vs ≤HS), CES Information(50)/PBS(60) employment, JOLTS openings/hires in those sectors, occupation via OEWS+CPS. Treatment break = ChatGPT Nov 30 2022.
- decision: Built `unemployment_pipeline/` package (config/registry/programs/bls_client/cache/crosswalks/exposure/datasets/quality/cli) + `dashboard/` (prepare/charts/build), TDD, 44 tests. Spec `docs/superpowers/specs/2026-06-08-bls-unemployment-pipeline-design.md`, plan `docs/superpowers/plans/2026-06-08-bls-pipeline-dashboard.md`.
- fact: First live pull = 785/809 series, 398k rows, 1948→May 2026. 28 missing = non-published metro SAE Info/PBS combos (graceful). Quality gate PASS. Run via `python -m unemployment_pipeline fetch`.
- fact: Registry is curated-rich (~809 series), expandable to full ~2,800 by extending `programs/constants.py` only.
- fact: Dashboard = `output/dashboard.html` (light editorial, Plotly via CDN, 7 exhibits). Build via `python -m dashboard.build`. KEY AI FINDINGS in data: education gap HS−Bach 2.82pp(2019-22)→1.79pp(2023+); Information employment −10.7% since Nov-2022 vs total nonfarm +3.1%.
- decision: API key stored in `.env` (gitignored). Key was pasted in chat → advised user to rotate.
- decision: Expanded coverage via a verification Workflow (4 parallel general-purpose agents live-verified series) → `data/reference/expanded_series.json` (175 series: 36 CES industries incl Software publishers/Data processing/Computer systems design, 62 JOLTS industry series, 77 CPS education-distribution + unemployment-by-industry). Loaded by `programs/expanded.py`. Registry now 931 series, 460k rows.
- fact: Expanded dashboard exhibits (10 total): industry employment leaderboard (`industry_employment_change`), education distribution stacked-area (`education_distribution`), JOLTS-by-industry openings (`jolts_industry_change`). All in `dashboard/prepare.py` + `charts.py`, keyed off `series_meta` tags so they auto-scale with coverage.
- finding: Biggest payroll declines since ChatGPT = motion picture/sound −28%, newspaper/publishers −20%, telecom −13.5%, Information −11%; gainers = education+health +12.8%. JOLTS openings: Information −61%. Graduate share of employment = 45.8% (bachelor's+).
- decision: Built INTERACTIVE dashboard (`dashboard/interactive_build.py` → `output/dashboard.html`) with a sticky baseline/compare date selector that recomputes leaderboard + demand scatter + sector index client-side (JS mirrors `bundle.change_since`). Embedded ~320KB monthly series JSON. New chart types in `charts_advanced.py`: industry×year heatmap, state-UR boxplot, animated choropleth (51 yearly frames). `bundle.py` builds embeddable series + matrices. 53 tests.
- finding (insight workflow, verified): Information employment peaked the EXACT month ChatGPT launched (2022-11), −332k/−10.7% since but only −3.7% vs 2019 → decline is post-ChatGPT. Computer systems design (5415) peaked the EXACT GPT-4 month (2023-03), −110k since. "Verdict flips with baseline" (software publishers +35% from 2019 vs flat from 2022) — justifies the date picker. Tech metros (SJ/SF/Seattle) inverted from ~1pp below national UR to at/above. Info openings −61% with ACTIVE layoffs +43% (not low-fire). Full insights: `data/reference/insights.json` + workflow output.
- note: Two insight/verification workflows used (ultracode) — `general-purpose` agents with Bash compute over parquet / verify series vs live API. Pattern works well.
- decision: Advanced-viz suite (`dashboard/advanced.py` + `charts_advanced.py`, integrated in `interactive_build.py`) replaces simple time series. 10 exhibits: contribution waterfall, change distribution (strip+box), exposure→change scatter (live OLS, slope −1.75/R²0.04), event-study Information-vs-control indexed to anchor, JOLTS freeze-vs-cuts quadrant, industry×year heatmap + diffusion index, state UR dispersion fan (LF-weighted), education gap, Beveridge curve, occupation bubble. Selected via a 5-lens design tournament (spec `docs/superpowers/specs/2026-06-09-advanced-viz-suite-design.md`).
- decision: CES leaf partition for waterfall/diffusion = `advanced.LEAF_PARTITION` (15 non-overlapping sectors, sums to total nonfarm; verified net +4760k since Nov-2022). Distribution/scatter use `DISPLAY_INDUSTRIES` (spotlights AI sub-industries). AIIE join needs sector-level aggregation by NAICS prefix (`_aiie_for`) — AIIE is 4-digit. JOLTS freeze-cuts joins openings↔layoffs by industry code (sid[3:9]), not label.
- gotcha: pandas `df.pct_change` is a METHOD — use `df["pct_change"]` for the column. Plotly `react` needs new arrays each call. CDP screenshots flake on the heavy 555KB page; verify charts via JS DOM (`div.data` traces) not screenshots.

